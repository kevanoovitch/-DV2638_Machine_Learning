### Assignment 1 run and setup instructions

Basically just run the junyper notebook called *assignment_2.ipynb* also the red wine dataset was used.



Dependencies are listed in `environment.yml`  
Create the environment with:
```bash
conda env create -f environment.yml
```